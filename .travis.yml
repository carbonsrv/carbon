language: go

go:
  - 1.3
  - 1.4
  - 1.5
  - tip

addons:
  apt:
    packages:
      - libluajit-5.1-dev
      - libphysfs-dev

notifications:
  irc:
    channels:
      - "irc.esper.net#vifino"
    use_notice: true
    template:
      - "\x0303%{repository_slug}\x0302#%{build_number} (\x0308%{branch}\x0F: \x0303%{commit}\x0F): %{author} - %{message}"
      - "Change view: %{compare_url}"
      - "Build details: %{build_url}"
